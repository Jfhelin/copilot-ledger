# Prompt Precision — merged into 01 (Round Trips Are the Lever)

> **This experiment was merged.** Prompt precision and context quality are the
> same mechanism — both decide how many discovery round trips the agent has to
> make — so they are now one article:
> [`01-context-quality.md`](01-context-quality.md) ("Round Trips Are the Lever").
> The precision angle lives there under **"The Same Lever: Prompt Precision."**

This stub is kept only to preserve the stable `03` ID and the existing
`site.js` / route references. Do **not** publish it as a standalone page.

If a dedicated precise-vs-vague capture is ever run, fold the result into 01's
precision section rather than reviving this file.
