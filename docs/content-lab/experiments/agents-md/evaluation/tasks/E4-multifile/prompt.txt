Add a product-count feature end to end. Expose a new API endpoint `GET /api/suppliers/:id/product-count` that returns the number of products belonging to that supplier, and surface that count in an appropriate place in the frontend. Wire it through the API client the frontend already uses, and keep the existing tests passing.
